name: cintermex_app
description: Movil App to buy tickets and gestion the events information.
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  carousel_slider: ^5.1.2
  supabase_flutter: ^2.12.0
  flutter_dotenv: ^5.2.1
  google_fonts: ^6.3.3
  intl: ^0.19.0
  provider: ^6.1.5
  url_launcher: ^6.3.2
  app_links: ^6.4.1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.4
  logging: ^1.3.0
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
